# Hello World の実行方法

このリポジトリには、`hello.py` という簡単な Python スクリプトが含まれています。このスクリプトを実行するとコンソールに `Hello World` と表示されます。

## 実行手順
1. ターミナルを開き、リポジトリのルートディレクトリに移動します。
2. 次のコマンドを実行します。
   ```bash
   python hello.py
   ```

成功すると、以下のように出力されます。

```text
Hello World
```

Python がインストールされていることを確認してください。インストールされていない場合は、公式サイトから Python をダウンロードしてインストールしてください。
